# CI Expectation

At this checkpoint the migration branch may remain RED because `tests/branding.test.mjs` intentionally requires VEYRA in production `index.html`. That RED is actionable evidence, not an APK/runtime failure. The next correction is the focused production branding edit followed by a complete rerun.
