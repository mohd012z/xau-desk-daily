# Next Gate

1. Migrate production `index.html` user-facing identity from XAU-DESK to VEYRA while preserving market semantics and functionality.
2. Run branding audit and full migration verifier.
3. Resolve repository rename-audit findings in runtime/deployment files.
4. Verify Pages and existing data/news workflows.
5. Open/refresh migration review and inspect CI evidence.
6. Only after all checks are green, approve the repository rename to `helix`.

After repository migration, connect the actual Android/Capacitor + Intelligence source and execute the separate VEYRA APK/runtime plan.
