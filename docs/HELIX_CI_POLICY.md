# HELIX / VEYRA CI Policy

Migration CI must run existing market/macro tests before deployment and also run migration-specific identity, rename, endpoint, secret, and orchestration checks.

A failing migration check is evidence to fix the branch, not a reason to weaken the test. Generated-output tests will be added when the actual APK web-preparation source is connected.
