# Production Index Branding Task

The remaining visible branding blocker is the large production `index.html`, currently carrying XAU-DESK identity.

Required edit:
- replace product title/brand/about identity with VEYRA;
- preserve legitimate `XAUUSD`/`XAU/USD` market references;
- preserve data IDs, calculations, navigation behavior, and existing scripts;
- run branding audit and full existing tests after the change.

This task should be a focused content-preserving edit, not a rewrite of the production dashboard.
