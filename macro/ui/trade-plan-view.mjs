function esc(v){return String(v??'—').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));}function join(xs){return(xs?.length?xs:['—']).map(esc).join(' • ');}export function renderTradePlanMarkup(p={}){const h=p.historical||{},q=p.quality||{label:'LOW',reasons:['INSUFFICIENT_DATA']},pv=p.pivotContext||{},pc=p.pivotConfluence||{};const range50=h.p25==null?'—':`${esc(h.p25)} → ${esc(h.p75)}`,range80=h.p10==null?'—':`${esc(h.p10)} → ${esc(h.p90)}`,reasons=q.reasons?.length?q.reasons.map(esc).join(' • '):'No active quality warnings';const pivotText=pv.levels?`P ${esc(pv.levels.pivot)} • R1 ${esc(pv.levels.r1)} • R2 ${esc(pv.levels.r2)} • R3 ${esc(pv.levels.r3)} • S1 ${esc(pv.levels.s1)} • S2 ${esc(pv.levels.s2)} • S3 ${esc(pv.levels.s3)}`:'Pivot data unavailable';let confluence=esc(pc.status||'INSUFFICIENT_DATA');if(pc.status==='CONFLUENCE')confluence=`${esc(pc.level)} ${esc(pc.price)} • CONFLUENCE`;if(pc.status==='DIVERGENCE_HOLD')confluence=`${esc(pc.level)} ${esc(pc.price)} • DIVERGENCE — PIVOT REFERENCE ON HOLD`;if(pc.status==='MIXED_HOLD')confluence=`${esc(pc.level)} ${esc(pc.price)} • MIXED — PIVOT REFERENCE ON HOLD`;return`<article class="trade-plan-card" data-model-state="${esc(p.modelState)}"><header><div><span class="eyebrow">MYT EVENT TRADE PLAN</span><h2>${esc(p.eventName)}</h2></div><strong>${esc(p.countdown?.text)}</strong></header><div class="trade-plan-time">${esc(p.eventTimeMyt)} • Asia/Kuala_Lumpur • ${esc(p.timeSource)} • ${esc(p.timeConfidence)}</div><div class="trade-plan-grid"><section><span>State</span><strong>${esc(p.modelState)}</strong></section><section><span>Affected</span><strong>${join(p.affectedAssets)}</strong></section><section><span>Model pressure</span><strong>${esc(p.pressure)}</strong></section><section><span>Observed reaction</span><strong>${esc(p.observed)}</strong></section><section><span>Confirmation</span><strong>${esc(p.confirmation)}</strong></section><section><span>From price</span><strong>${esc(p.prices?.from)}</strong></section></div><div class="trade-plan-ranges"><span>Historical n ${esc(h.n??'—')}</span><span>Median ${esc(h.median??'—')}</span><span>50% ${range50}</span><span>80% ${range80}</span></div><div class="trade-plan-pivots"><strong>Pivot reference • ${esc(pv.timeframe||'—')} • ${esc(pv.source||'—')}</strong><span>${pivotText}</span><span>${confluence}</span></div><footer><strong>Quality ${esc(q.label)}</strong><span>${reasons}</span></footer></article>`;}