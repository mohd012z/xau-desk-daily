function quantile(sorted,q){const pos=(sorted.length-1)*q,lo=Math.floor(pos),hi=Math.ceil(pos);if(lo===hi)return sorted[lo];return sorted[lo]+(sorted[hi]-sorted[lo])*(pos-lo);}export function summarizeDistribution(samples){const xs=(samples||[]).map(Number).filter(Number.isFinite).sort((a,b)=>a-b);if(!xs.length)return null;return{n:xs.length,median:quantile(xs,.5),p10:quantile(xs,.1),p25:quantile(xs,.25),p75:quantile(xs,.75),p90:quantile(xs,.9)};}const clean=n=>Number(Number(n).toPrecision(15)),validD=d=>d&&['median','p10','p25','p75','p90'].every(k=>Number.isFinite(Number(d[k])));export function fxBandFromPips(prePrice,pipSize,d){const pre=Number(prePrice),pip=Number(pipSize);if(!validD(d)||!Number.isFinite(pre)||!Number.isFinite(pip)||pip<=0)return null;const at=p=>clean(pre+Number(p)*pip);return{center:at(d.median),band50:[at(d.p25),at(d.p75)],band80:[at(d.p10),at(d.p90)]};}export function percentBandFromReturns(prePrice,d){const pre=Number(prePrice);if(!validD(d)||!Number.isFinite(pre)||pre<=0)return null;const at=p=>clean(pre*(1+Number(p)/100));return{center:at(d.median),band50:[at(d.p25),at(d.p75)],band80:[at(d.p10),at(d.p90)]};}