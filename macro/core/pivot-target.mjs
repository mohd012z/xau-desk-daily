const clean=n=>Number(Number(n).toPrecision(15));export function classicPivots({high,low,close}){const H=Number(high),L=Number(low),C=Number(close);if(![H,L,C].every(Number.isFinite)||H<=L)return null;const p=(H+L+C)/3;return{pivot:clean(p),r1:clean(2*p-L),s1:clean(2*p-H),r2:clean(p+(H-L)),s2:clean(p-(H-L)),r3:clean(H+2*(p-L)),s3:clean(L-2*(H-p))};}export function findPivotConfluence({pivots,band,pressure='MIXED',referencePrice=null}){if(!pivots||!Array.isArray(band)||band.length<2)return{status:'INSUFFICIENT_DATA',level:null,price:null};const nums=band.map(Number);if(!nums.every(Number.isFinite))return{status:'INSUFFICIENT_DATA',level:null,price:null};const lo=Math.min(...nums),hi=Math.max(...nums),ref=Number(referencePrice);if(pressure!=='MIXED'&&!Number.isFinite(ref))return{status:'INSUFFICIENT_REFERENCE_PRICE',level:null,price:null};let names=pressure==='DOWN_PRESSURE'?['s1','s2','s3']:pressure==='UP_PRESSURE'?['r1','r2','r3']:['pivot','s1','r1','s2','r2','s3','r3'];if(Number.isFinite(ref))names=names.filter(n=>pressure==='DOWN_PRESSURE'?Number(pivots[n])<ref:pressure==='UP_PRESSURE'?Number(pivots[n])>ref:true);const matches=names.map(name=>({name,price:Number(pivots[name])})).filter(x=>Number.isFinite(x.price)&&x.price>=lo&&x.price<=hi);if(!matches.length)return{status:'NO_PIVOT_CONFLUENCE',level:null,price:null};const center=(lo+hi)/2;matches.sort((a,b)=>Math.abs(a.price-center)-Math.abs(b.price-center));return{status:'CONFLUENCE',level:matches[0].name.toUpperCase(),price:matches[0].price};}