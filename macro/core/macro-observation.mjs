const freeze=v=>{if(!v||typeof v!=='object'||Object.isFrozen(v))return v;for(const x of Object.values(v))freeze(x);return Object.freeze(v);};
export function aggregateMacroObservation({events=[]}={}){if(!Array.isArray(events))throw new TypeError('events must be array');let state='ALLOW';const refs=[],reasons=[];for(const e of events){if(!e?.xau_relevant)continue;refs.push(e.event_ref??null);reasons.push(...(e.reason_codes??[]));if(e.risk==='BLOCK')state='BLOCK';else if(e.risk==='WATCH_ONLY'&&state!=='BLOCK')state='WATCH_ONLY';else if(!['NONE','WATCH_ONLY','BLOCK'].includes(e.risk)){state='BLOCK';reasons.push('MALFORMED_RELEVANT_EVENT_RISK');}}return freeze({state,event_refs:refs,reason_codes:[...new Set(reasons)]});}
