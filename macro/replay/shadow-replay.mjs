import{normalizeReplayRecord}from'./replay-record.mjs';import{runBbmaNewsShadow}from'../bbma/news-shadow-pipeline.mjs';
export function replayShadowRecords(records){if(!Array.isArray(records))throw new TypeError('records must be array');return records.map(normalizeReplayRecord).sort((a,b)=>a.observedUtc.localeCompare(b.observedUtc)||a.record_id.localeCompare(b.record_id)).map(r=>{const observation=runBbmaNewsShadow({symbol:r.symbol,evidence:r.evidence,events:r.events,eventPolicy:r.eventPolicy,observedUtc:r.observedUtc,healthInput:r.healthInput,signal:r.signal});return Object.freeze({replay_id:r.record_id,source_ref:r.source_ref,observed_utc:r.observedUtc,observation});});}
