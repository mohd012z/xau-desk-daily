const clone=v=>Array.isArray(v)?v.map(clone):v&&typeof v==='object'?Object.fromEntries(Object.entries(v).map(([k,x])=>[k,clone(x)])):v;const freeze=v=>{if(!v||typeof v!=='object'||Object.isFrozen(v))return v;for(const x of Object.values(v))freeze(x);return Object.freeze(v);};
export function createObservationLedger(){const records=[],ids=new Set();let duplicates=0;return Object.freeze({append(record){if(!record||typeof record.replay_id!=='string')throw new TypeError('replay_id required');if(ids.has(record.replay_id)){duplicates++;return false;}ids.add(record.replay_id);records.push(clone(record));return true;},snapshot(){return freeze({records:records.slice().sort((a,b)=>String(a.observed_utc).localeCompare(String(b.observed_utc))||a.replay_id.localeCompare(b.replay_id)).map(clone),exact_duplicate_count:duplicates});}});}
