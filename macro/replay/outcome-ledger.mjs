const freeze=v=>{if(!v||typeof v!=='object'||Object.isFrozen(v))return v;for(const x of Object.values(v))freeze(x);return Object.freeze(v);};const utc=v=>typeof v==='string'&&v.endsWith('Z')&&!Number.isNaN(Date.parse(v));
export function createOutcomeLedger(observations=[]){const index=new Map(observations.map(x=>[x.replay_id,x]));const outcomes=new Map();return Object.freeze({attach({replay_id,horizon,outcome_utc,entry_price,forward_price,mfe=null,mae=null}){const base=index.get(replay_id);if(!base)throw new TypeError('unknown replay_id');if(typeof horizon!=='string'||!horizon||!utc(outcome_utc)||Date.parse(outcome_utc)<=Date.parse(base.observed_utc))throw new TypeError('valid forward outcome required');if(!Number.isFinite(entry_price)||!Number.isFinite(forward_price))throw new TypeError('finite prices required');const key=`${replay_id}|${horizon}`;const row=freeze({replay_id,horizon,outcome_utc,entry_price,forward_price,mfe,mae});outcomes.set(key,row);return row;},snapshot(){return freeze([...outcomes.values()].sort((a,b)=>a.replay_id.localeCompare(b.replay_id)||a.horizon.localeCompare(b.horizon)));}});}
