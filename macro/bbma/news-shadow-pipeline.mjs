import{normalizeXauEventRisk}from'../adapters/xau-event-risk.mjs';import{aggregateMacroObservation}from'../core/macro-observation.mjs';import{evaluateDataHealth}from'../core/data-health.mjs';import{evaluateShadowRiskGate}from'../core/shadow-risk-gate.mjs';import{evaluateBbmaConfluence}from'./confluence.mjs';import{composeBbmaShadowCandidate}from'./shadow-candidate.mjs';import{advanceBbmaShadowSignal}from'./lifecycle-bridge.mjs';import{composeNewsShadowObservation}from'./news-shadow-observation.mjs';
export function runBbmaNewsShadow({symbol='XAUUSD',evidence,events=[],eventPolicy,observedUtc,healthInput,signal}){const normalized=events.map(event=>normalizeXauEventRisk({event,observedUtc,policy:eventPolicy}));const macroObservation=aggregateMacroObservation({events:normalized});const dataHealth=evaluateDataHealth({...healthInput,nowUtc:observedUtc});const gate=evaluateShadowRiskGate({macroObservation,dataHealth});const confluence=evaluateBbmaConfluence({evidence});const candidate=composeBbmaShadowCandidate({symbol,evidence,confluence,gate,generatedUtc:observedUtc,existingSignal:signal});const lifecycle=advanceBbmaShadowSignal({signal,confluence,gate,atUtc:observedUtc});return composeNewsShadowObservation({candidate,macroObservation,dataHealth,gate,lifecycle,generatedUtc:observedUtc});}
