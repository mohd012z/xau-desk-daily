import test from'node:test';import assert from'node:assert/strict';import{summarizeDistribution,fxBandFromPips,percentBandFromReturns}from'../../macro/core/distribution.mjs';
test('summarizes signed event samples with 50 and 80 percent bands',()=>{const d=summarizeDistribution([-20,-10,0,10,20]);assert.equal(d.n,5);assert.equal(d.median,0);assert.equal(d.p25,-10);assert.equal(d.p75,10);assert.equal(d.p10,-16);assert.equal(d.p90,16);});
test('converts FX pip distribution to exact price bands',()=>{const d=summarizeDistribution([-20,-10,0,10,20]);const b=fxBandFromPips(1.1,.0001,d);assert.deepEqual(b.band50,[1.099,1.101]);assert.deepEqual(b.band80,[1.0984,1.1016]);});
test('converts percentage return distribution to price bands and handles empty samples',()=>{assert.equal(summarizeDistribution([]),null);const d=summarizeDistribution([-2,-1,0,1,2]);const b=percentBandFromReturns(2000,d);assert.deepEqual(b.band50,[1980,2020]);assert.deepEqual(b.band80,[1968,2032]);});
test('invalid pre-price or pip size fails closed',()=>{const d=summarizeDistribution([-1,0,1]);assert.equal(fxBandFromPips('bad',.0001,d),null);assert.equal(fxBandFromPips(1.1,0,d),null);assert.equal(percentBandFromReturns('bad',d),null);});
test('non-finite distribution fields fail closed during price conversion',()=>{const bad={n:3,median:NaN,p10:-1,p25:-.5,p75:.5,p90:1};assert.equal(fxBandFromPips(1.1,.0001,bad),null);assert.equal(percentBandFromReturns(100,bad),null);});