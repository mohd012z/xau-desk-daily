import test from'node:test';import assert from'node:assert/strict';import{assessPlanQuality}from'../../macro/core/plan-quality.mjs';
test('exposes low-quality reasons',()=>{const q=assessPlanQuality({effectiveSampleSize:5,timeConfidence:'LOW',feedState:'STALE',modelAgreement:'DISAGREE'});assert.equal(q.label,'LOW');assert.ok(q.reasons.includes('LOW_SAMPLE_SIZE'));assert.ok(q.reasons.includes('LOW_TIMESTAMP_CONFIDENCE'));assert.ok(q.reasons.includes('STALE_FEED'));assert.ok(q.reasons.includes('MODEL_DISAGREEMENT'));});
test('strong inputs do not invent quality problems',()=>{assert.deepEqual(assessPlanQuality({effectiveSampleSize:80,timeConfidence:'HIGH',feedState:'STREAMING',modelAgreement:'AGREE'}),{label:'HIGH',reasons:[]});});
test('snapshot feed is explicit quality limitation',()=>{const q=assessPlanQuality({effectiveSampleSize:80,timeConfidence:'HIGH',feedState:'SNAPSHOT',modelAgreement:'AGREE'});assert.equal(q.label,'MODERATE');assert.ok(q.reasons.includes('SNAPSHOT_FEED'));});
test('unknown feed state is not treated as high quality',()=>{const q=assessPlanQuality({effectiveSampleSize:80,timeConfidence:'HIGH',feedState:'UNKNOWN',modelAgreement:'AGREE'});assert.equal(q.label,'MODERATE');assert.ok(q.reasons.includes('UNKNOWN_FEED_STATE'));});
test('unknown model agreement is disclosed',()=>{const q=assessPlanQuality({effectiveSampleSize:80,timeConfidence:'HIGH',feedState:'STREAMING',modelAgreement:'UNKNOWN'});assert.equal(q.label,'MODERATE');assert.ok(q.reasons.includes('UNKNOWN_MODEL_AGREEMENT'));});
test('unknown timestamp confidence is disclosed',()=>{const q=assessPlanQuality({effectiveSampleSize:80,timeConfidence:'UNKNOWN',feedState:'STREAMING',modelAgreement:'AGREE'});assert.equal(q.label,'MODERATE');assert.ok(q.reasons.includes('UNKNOWN_TIMESTAMP_CONFIDENCE'));});
test('invalid effective sample size is low quality',()=>{const q=assessPlanQuality({effectiveSampleSize:'bad',timeConfidence:'HIGH',feedState:'STREAMING',modelAgreement:'AGREE'});assert.equal(q.label,'LOW');assert.ok(q.reasons.includes('INVALID_EFFECTIVE_SAMPLE_SIZE'));});
test('delayed feed is disclosed as moderate quality',()=>{const q=assessPlanQuality({effectiveSampleSize:80,timeConfidence:'HIGH',feedState:'DELAYED',modelAgreement:'AGREE'});assert.equal(q.label,'MODERATE');assert.ok(q.reasons.includes('DELAYED_FEED'));});
test('market closed is disclosed rather than treated as streaming quality',()=>{const q=assessPlanQuality({effectiveSampleSize:80,timeConfidence:'HIGH',feedState:'MARKET CLOSED',modelAgreement:'AGREE'});assert.equal(q.label,'MODERATE');assert.ok(q.reasons.includes('MARKET_CLOSED'));});