import test from'node:test';import assert from'node:assert/strict';import{composeNewsShadowObservation}from'../../macro/bbma/news-shadow-observation.mjs';
const candidate={symbol:'XAUUSD',direction:'BUY',readiness:'CONFIRMABLE',state:'SETUP',supporting_timeframes:['H4','H1','M15','M5'],conflict_timeframes:[],reason_codes:['DIRECTION_BUY']};const macro={state:'WATCH_ONLY',event_refs:['e1'],reason_codes:['HIGH_IMPACT_UPCOMING']};const health={reasons:[],technical_confirmation_allowed:true,macro_state:'AVAILABLE'};const gate={state:'WATCH_ONLY',reason_codes:['MACRO_WATCH_ONLY']};const lifecycle={action:'HOLD',reason:'NO_LEGAL_PROGRESS',signal:{state:'SETUP'}};const at='2026-09-24T13:30:00.000Z';
test('preserves BBMA direction while exposing macro hold',()=>{const o=composeNewsShadowObservation({candidate,macroObservation:macro,dataHealth:health,gate,lifecycle,generatedUtc:at});assert.equal(o.direction,'BUY');assert.equal(o.gate.state,'WATCH_ONLY');assert.equal(o.state,'SETUP');assert.equal(Object.isFrozen(o),true);assert.equal(Object.isFrozen(o.candidate),true);});
test('identical explicit inputs replay deterministically',()=>{const args={candidate,macroObservation:macro,dataHealth:health,gate,lifecycle,generatedUtc:at};assert.deepEqual(composeNewsShadowObservation(args),composeNewsShadowObservation(args));});
